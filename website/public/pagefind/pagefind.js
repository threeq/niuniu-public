// Dev-time stub. The real pagefind/pagefind.js is generated into dist/pagefind
// by `pnpm build` (astro build && pagefind --site dist). During `pnpm dev`
// this stub keeps the dynamic import in SearchDialog.tsx resolvable so the
// page does not error out.
export async function search() {
  return { results: [] };
}
export default { search };
