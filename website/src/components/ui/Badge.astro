---
interface Props { class?: string; }
const { class: className = '' } = Astro.props;
---
<span class:list={['inline-flex items-center gap-1 rounded-full bg-[rgb(var(--color-card))] border border-[rgb(var(--color-border))] px-2 py-0.5 text-xs', className]}>
  <slot />
</span>
